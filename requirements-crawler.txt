# 爬虫模块依赖

# 核心依赖
aiohttp>=3.9.0  # 异步HTTP客户端
beautifulsoup4>=4.12.0  # HTML解析
lxml>=5.0.0  # 更快的HTML解析器

# 性能优化
tenacity>=8.2.3  # 重试机制
aiolimiter>=1.1.0  # 速率限制
aiocache>=0.12.2  # 异步缓存

# 可选: 浏览器自动化（用于反爬虫）
# playwright>=1.40.0  # 浏览器自动化
# selenium>=4.16.0  # 传统浏览器自动化

# 代理支持
aiohttp-socks>=0.11.0  # SOCKS代理支持

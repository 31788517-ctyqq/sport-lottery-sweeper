"""
Core package initialization.

This file can be used to expose key components from the core submodules
for easier imports at higher levels.
"""
# Example of re-exporting key classes/functions if needed:
# from .config import get_settings
# from .database import get_db_session

# For now, it's just an empty file to mark this directory as a Python package.
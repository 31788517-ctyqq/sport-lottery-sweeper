# 生产环境配置
[app]
debug = false
log_level = "INFO"
database_url = "postgresql://user:password@localhost/prod_sport_lottery"

[scraper]
scrape_interval = 1800  # 30分钟
max_retry = 3

[cache]
enabled = true
ttl = 1800  # 30分钟